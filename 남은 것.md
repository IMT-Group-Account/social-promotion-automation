# SNS 자동화 프로그램 남은 작업

작성 기준일: 2026-09-23  
기준 저장소: `social-promotion-automation`

## 1. 현재 결론

애플리케이션 코드와 로컬 검증은 완료됐지만, 실제 서비스를 열기 위해서는
운영 인프라와 외부 사업자 계정 연결이 필요하다. 이 저장소에는 실제 운영
비밀값을 저장하지 않으며, 운영 DB 마이그레이션·SNS 게시·배포도 아직 실행하지
않았다.

배포 기본 방향은 **Docker 비의존 방식**이다.

```text
Vercel 또는 정적/Node 호스팅의 관리자 프런트엔드
  -> HTTPS reverse proxy
  -> Oracle VM의 Node.js API(systemd)
  -> Node.js publish worker(systemd)
  -> Node.js analytics scheduler(systemd)
  -> PostgreSQL + Redis
```

PostgreSQL과 Redis는 관리형 서비스를 우선 권장한다. 직접 운영해야 한다면
Oracle VM에 OS 패키지로 설치하고 각각 `systemd` 서비스로 관리한다. Docker
Compose 파일은 선택적 대안일 뿐이며 기본 배포 절차에는 필요하지 않다.

## 2. 완료된 항목

- 캠페인·초안·승인·예약·취소·복사·플랫폼별 재시도 구현
- SNS별 독립 게시 작업과 중복 게시 방지 구현
- Facebook, Instagram, Threads, LinkedIn, X 어댑터 및 Kakao Channel 연결 구현
- OAuth state 검증과 AES-GCM 토큰 암호화·키 순환 구현
- 서비스 JWT의 RS256/ES256 및 JWKS 검증 구현
- editor, publisher, admin 역할 기반 접근 제어 구현
- 미디어 실제 파일 형식·크기 검증과 X 최대 4개 미디어 처리 구현
- 플랫폼별 분석 수집·대시보드·원시 지표 분리 구현
- 감사 로그, 실패 알림 outbox, Webhook 전달 및 운영 화면 구현
- API, worker, scheduler heartbeat와 `/api/health/ready` 구현
- 마이그레이션 `001`부터 `016`까지 실제 PostgreSQL 적용 검증
- 백엔드 테스트 72개 및 PostgreSQL 통합 시나리오 10개 통과
- 백엔드·프런트엔드 lint, typecheck, production build 통과
- 백엔드·프런트엔드 전체 `npm audit` 취약점 0건
- CI에 의존성 감사, lint, typecheck, test, build 추가
- API·worker·scheduler용 네이티브 `systemd` 템플릿 추가
- Windows에서 설치 가능한 관리자 PWA manifest, 앱 아이콘, 설치 안내 추가
- 저장소 최상단 `start.cmd`와 실제 SNS 비연결 로컬 데모 진입점 추가

현재 변경은 로컬 작업 트리에 반영돼 있다. 커밋, GitHub push, 운영 배포는 아직
실행하지 않았다.

## 3. 현재 확인된 외부 준비 상태

- 실제 `.env`, `.env.production`, `infrastructure/oracle/backend.env` 없음
- 이 PC에 Docker, Redis 서버/CLI, Vercel CLI 없음
- GitHub 원격 저장소 접근 가능
- GitHub Actions에는 CI만 있고 배포 workflow는 없음
- GitHub Actions 저장소 secret 및 variable이 등록돼 있지 않음
- 운영 서버 주소, SSH 접속 대상, 운영 도메인 정보 없음
- 실제 PostgreSQL 및 Redis 접속 정보 없음
- 인증 서비스 issuer, audience, JWKS 주소 없음
- SNS 사업자 앱 ID·secret·승인 상태 없음

따라서 아래 작업은 임의의 예시값으로 완료 처리할 수 없다. 실제 대상과
승인된 계정이 제공돼야 한다.

## 4. Docker 없이 운영하기 위해 필요한 서버 준비

### 4.1 운영 서버

필요 정보:

- Oracle VM 또는 동등한 Linux 서버의 호스트명/IP
- SSH 접속 계정과 승인된 인증 방식
- 운영 API 도메인
- 관리자 프런트엔드 도메인
- DNS 변경 권한

서버 준비:

1. Node.js 20 이상을 설치한다.
2. `social-promotion` 전용 시스템 계정을 생성한다.
3. `/opt/social-promotion/releases/<release-id>`에 릴리스를 배치한다.
4. `/opt/social-promotion/current`를 현재 릴리스 symlink로 사용한다.
5. `/etc/social-promotion/backend.env`에 운영 비밀값을 저장한다.
6. `/var/lib/social-promotion/uploads`를 영구 미디어 저장소로 사용한다.
7. `infrastructure/oracle/systemd`의 세 unit을 설치한다.
8. Nginx, Caddy 또는 기존 로드밸런서에서 API의 3000번 포트를 HTTPS로
   역방향 프록시한다. 3000번 포트는 인터넷에 직접 공개하지 않는다.

완료 기준:

- `systemctl is-active`에서 API, worker, scheduler가 모두 `active`
- `/api/health`가 HTTP 200
- `/api/health/ready`가 HTTP 200
- 세 프로세스의 `RELEASE_ID`가 동일
- 재부팅 후 세 서비스가 자동 복구
- 애플리케이션 계정이 운영 비밀 파일을 수정하거나 다른 사용자 홈을 읽을 수 없음

### 4.2 PostgreSQL

권장 방식은 자동 백업과 TLS를 제공하는 관리형 PostgreSQL이다. VM에 직접
설치할 경우 OS 서비스로 실행하고 애플리케이션과 DB 계정을 분리해야 한다.

필요 작업:

- 운영 전용 데이터베이스와 최소 권한 애플리케이션 계정 생성
- TLS 또는 사설망 접속 적용
- 자동 백업, 보존 기간, 복원 훈련 정의
- 배포 전 마이그레이션 `001`~`016`의 적용 계획 확인
- migration 013 전 기존 worker·scheduler 중지 및 처리 중 작업 확인
- 백업 ID 기록 후 승인된 단일 migration runner로 한 번만 적용

완료 기준:

- 모든 마이그레이션 적용 이력과 checksum 기록
- 애플리케이션 계정으로 destructive 관리 작업 불가
- 실제 백업에서 별도 DB로 복원 검증 완료
- API·worker·scheduler가 동일한 운영 DB에 연결

### 4.3 Redis

Redis는 BullMQ 예약 게시에 필요하다. Docker가 없어도 관리형 Redis나 OS
서비스 Redis를 사용할 수 있다.

필수 조건:

- `redis://` 사설망 또는 `rediss://` TLS 접속
- 인증 활성화
- 인터넷 직접 공개 금지
- 메모리 제한과 적절한 eviction 정책 검토
- 운영 모니터링과 재시작 정책 설정

완료 기준:

- worker에서 연결 성공
- 예약 작업이 Redis에 들어간 후 정해진 시간에 단 한 번 처리
- Redis 일시 중단 후 DB outbox를 통해 누락 없이 복구
- sibling SNS 게시 작업의 상태가 서로 오염되지 않음

## 5. 운영 환경 변수

실제 값은 채팅, Git, 로그에 남기지 않는다. 서버의
`/etc/social-promotion/backend.env` 또는 승인된 secret manager에 저장한다.

최소 공통 설정:

```text
DATABASE_URL
REDIS_URL
PUBLIC_API_ORIGIN
ADMIN_CONSOLE_ORIGIN
CORS_ORIGINS
MEDIA_STORAGE_DIR
MEDIA_PUBLIC_BASE_URL
OAUTH_TOKEN_ENCRYPTION_KEY_VERSION
OAUTH_TOKEN_ENCRYPTION_KEYS
SERVICE_JWT_ISSUER
SERVICE_JWT_AUDIENCE
SERVICE_JWT_SIGNING_ALGORITHM
SERVICE_JWT_JWKS_URL
SERVICE_RBAC_ENABLED=true
SERVICE_JWT_ROLES_CLAIM
RELEASE_ID
```

선택적 운영 알림:

```text
FAILURE_ALERT_WEBHOOK_URL
FAILURE_ALERT_WEBHOOK_TOKEN
```

완료 기준:

```bash
npm run check:editorial
```

위 명령이 오류 없이 끝나고 `/api/health/ready`가 200이어야 한다.

## 6. 인증 서비스 연결

현재 API는 임의의 공유 secret JWT를 허용하지 않는다. 인증 서비스가 RS256
또는 ES256으로 토큰을 발급하고 공개키를 HTTPS JWKS로 제공해야 한다.

필요 작업:

- 운영 issuer와 audience 확정
- HTTPS JWKS URL 제공
- JWT `sub`에 내부 사용자 식별자 포함
- 설정한 roles claim에 `editor`, `publisher`, `admin` 중 승인된 역할 포함
- signing key rotation 절차 검증
- 만료, 잘못된 audience, 알 수 없는 `kid`, 변조 토큰 거부 확인

완료 기준:

- editor는 초안 작업만 가능
- publisher는 승인된 게시·예약·재시도 가능
- admin은 연결 설정과 운영 감사 화면 접근 가능
- 권한이 없는 토큰은 403, 유효하지 않은 토큰은 401

## 7. SNS 사업자 연결

사업자 콘솔에서 앱 생성, callback URL 등록, 심사 및 테스트 계정 준비가 필요하다.
앱 ID와 secret은 저장소에 넣지 않는다.

| 플랫폼 | 필요한 외부 준비 | 실제 완료 판정 |
| --- | --- | --- |
| LinkedIn | OIDC, 게시 scope, 개인/조직 권한, API version | 비공개 테스트 게시 후 원격 ID·URL 조회 |
| Facebook | Meta 앱, Page 선택, `pages_manage_posts` 등 승인 scope | 선택한 테스트 Page에 게시·조회·삭제 |
| Instagram | Facebook Page와 Professional Instagram 연결, publish scope | 이미지 container 생성·완료·게시 확인 |
| Threads | 별도 Threads OAuth 앱과 publish scope | 테스트 계정 게시 후 원격 조회 |
| X | OAuth 2.0, `tweet.write`, 미디어 권한, 과금 한도 승인 | 예상 비용 제한 내 비공개 테스트 게시·조회·삭제 |
| Kakao Channel | 승인된 채널 URL과 integration key | 임의 URL 차단 및 승인 채널 이동·상담 이벤트 확인 |

공통 callback 형식은 `docs/oauth-security.md`를 따른다. 정적 설정 검사는
플랫폼마다 실행한다.

```bash
npm run check:provider -- linkedin
npm run check:provider -- facebook
npm run check:provider -- instagram
npm run check:provider -- threads
npm run check:provider -- x
```

이 검사가 통과해도 실제 OAuth 동의, 앱 심사, 테스트 게시 성공을 대신하지
않는다. 실제 게시 검증은 승인된 비공개 계정과 삭제 가능한 테스트 콘텐츠로
플랫폼 하나씩 수행한다.

## 8. 미디어 공개 경로

SNS 사업자 서버가 미디어를 가져가려면 `MEDIA_PUBLIC_BASE_URL`이 공인 HTTPS로
접근 가능해야 한다.

필요 작업:

- `/var/lib/social-promotion/uploads` 백업
- API의 `/api/media/files/<approved-file>` 경로만 공개
- 실행 파일, 잘못된 MIME, 경로 탈출 요청 차단 확인
- SNS 사업자 네트워크에서 이미지와 MP4 다운로드 가능 여부 확인
- 프록시 body size와 애플리케이션 업로드 한도 정렬

완료 기준:

- 인증된 관리자만 업로드 가능
- 승인된 UUID 파일만 공개 조회 가능
- HTTPS 인증서와 MIME이 정상
- 운영 SNS가 실제 파일을 수집해 게시 성공

## 9. 프런트엔드 배포

관리자 화면은 Chrome·Edge에서 Windows 앱처럼 설치 가능한 PWA로 구현됐다.
설치 앱은 서버 비밀값이나 게시 큐를 내장하지 않고 운영 HTTPS 주소에 연결한다.
현재 Vercel CLI와 Vercel 프로젝트 연결 정보가 없다. Vercel을 사용할 경우
다음 정보가 필요하다.

- 대상 Vercel team/project
- 운영 관리자 도메인
- `APP_ORIGIN`, `BACKEND_API_URL`, OAuth callback origin
- GitHub 연결 또는 승인된 배포 방식

프런트엔드에는 SNS secret, DB URL, Redis URL을 설정하지 않는다.
`NEXT_PUBLIC_*`에는 브라우저 공개가 허용된 값만 둔다.

완료 기준:

- 운영 HTTPS 주소에서 PWA 설치 버튼과 브라우저 설치 아이콘 표시
- 시작 메뉴와 작업 표시줄에서 독립 창으로 실행
- 로그인 쿠키가 `HttpOnly`, `Secure`, 적절한 `SameSite`로 발급
- BFF allowlist 밖의 경로가 404
- 신뢰하지 않는 origin의 변경 요청이 403
- 관리자 화면에서 초안·승인·예약·분석·운영 로그가 정상 표시

## 10. 배포 및 실제 E2E 순서

1. 운영 대상과 접근 권한을 확인한다.
2. PostgreSQL과 Redis를 준비한다.
3. 인증 서비스 JWT/JWKS를 연결한다.
4. API 도메인과 HTTPS reverse proxy를 준비한다.
5. 운영 환경 변수의 정적 준비도 검사를 통과시킨다.
6. worker와 scheduler를 중지한다.
7. DB 백업 ID와 migration plan을 기록한다.
8. 마이그레이션을 한 번만 적용한다.
9. 검증된 새 release symlink를 활성화한다.
10. API, worker, scheduler 순서로 시작한다.
11. liveness와 readiness, 로그, heartbeat를 확인한다.
12. 플랫폼 하나씩 OAuth와 비공개 게시 E2E를 수행한다.
13. 실패 알림, Redis 장애 복구, 모호한 provider 결과의 fail-closed 동작을 확인한다.
14. 증거와 rollback 기준을 release log에 기록한다.

## 11. 다음 진행을 위해 필요한 사용자 제공 사항

비밀값 자체를 채팅으로 전달하지 말고, 접근 가능한 대상이나 로그인된 브라우저
세션을 지정해야 한다.

- 운영 Linux 서버의 승인된 SSH 대상
- PostgreSQL 및 Redis를 관리형으로 사용할지 서버에 직접 설치할지 결정
- API와 관리자 화면에 사용할 도메인
- 인증 서비스와 JWKS 관리 주체
- LinkedIn, Meta, Threads, X, Kakao 관리자 콘솔 접근
- 테스트 게시가 허용된 비공개 계정·Page·조직
- Vercel 사용 여부와 대상 project
- 운영 DB 마이그레이션 및 실제 SNS 테스트 게시 승인

위 항목이 준비되기 전에는 예시 주소나 가짜 credential로 운영 준비 완료를
표시하지 않는다. 준비된 항목부터 `docs/staging-provider-verification.md`의
증거 형식으로 하나씩 완료 처리한다.
