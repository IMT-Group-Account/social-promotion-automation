# Public and safe to expose to the browser: the Oracle API HTTPS origin plus /api.
# Do not put OAuth tokens, client secrets, DATABASE_URL, or REDIS_URL in this file.
NEXT_PUBLIC_API_BASE_URL=https://api.example.com/api
